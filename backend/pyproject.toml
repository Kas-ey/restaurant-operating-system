[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "restaurant-operating-system"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = ["Flask>=3.0", "Flask-SQLAlchemy>=3.5", "Flask-Migrate>=4.0", "SQLAlchemy>=2.0"]
